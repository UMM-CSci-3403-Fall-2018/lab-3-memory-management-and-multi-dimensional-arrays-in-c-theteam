#include <stdlib.h>

#include "array_merge.h"
#include "mergesort.h"

int* array_merge(int num_arrays, int* sizes, int** values) {
//	int* unique_values = (int*) malloc( 

	for(int i = 0; i < num_arrays, i++) {
	
		for(int j = 0; j < sizes[i]; j++){

	}
}
}

int total_elements(int* sizes) {
	int total_elements = 0;

	for(int i = 0; i < s
}

